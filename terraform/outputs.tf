# Root outputs for the main stack.
#
# Empty in the PR-006 skeleton. As modules are migrated (PR-007..PR-015), surface
# the useful values here — e.g. bucket names (storage), the state machine ARN
# (orchestration), the alerts topic ARN (observability).
#
# TODO PR-007+: add outputs as modules land.
