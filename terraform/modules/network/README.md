# Module: `network`

VPC, subnets, route tables, NAT/IGW (gated by var.enable_internet), S3 VPC endpoint, and the SageMaker security group.

**Status:** placeholder skeleton created in **PR-006**. Resources land here in **PR-008**
(migrated from `terraform-lottery/Prod/` via `terraform state mv`, so no resource is
recreated).

## Files
- `main.tf` — resources (empty until PR-008)
- `variables.tf` — module inputs
- `outputs.tf` — module outputs
