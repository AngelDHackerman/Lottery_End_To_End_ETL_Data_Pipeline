# Module: network
# Intent: VPC, subnets, route tables, NAT/IGW (gated by var.enable_internet), S3 VPC endpoint, and the SageMaker security group.
#
# STATUS: placeholder skeleton (PR-006). No resources yet.
# Resources are migrated here from terraform-lottery/Prod/ in PR-008 via
# `terraform state mv` (no resource churn). See roadmap.md for the exact plan.

# TODO PR-008: move this module's resources in and wire the root caller.
