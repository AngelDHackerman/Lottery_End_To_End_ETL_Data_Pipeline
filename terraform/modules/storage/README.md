# Module: `storage`

The imported prod data buckets (partitioned + simple), plus the Athena results and Lambda code buckets, with versioning / SSE / prevent_destroy.

**Status:** placeholder skeleton created in **PR-006**. Resources land here in **PR-007**
(migrated from `terraform-lottery/Prod/` via `terraform state mv`, so no resource is
recreated).

## Files
- `main.tf` — resources (empty until PR-007)
- `variables.tf` — module inputs
- `outputs.tf` — module outputs
