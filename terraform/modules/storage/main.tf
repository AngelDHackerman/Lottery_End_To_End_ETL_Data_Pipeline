# Module: storage
# Intent: The imported prod data buckets (partitioned + simple), plus the Athena results and Lambda code buckets, with versioning / SSE / prevent_destroy.
#
# STATUS: placeholder skeleton (PR-006). No resources yet.
# Resources are migrated here from terraform-lottery/Prod/ in PR-007 via
# `terraform state mv` (no resource churn). See roadmap.md for the exact plan.

# TODO PR-007: move this module's resources in and wire the root caller.
