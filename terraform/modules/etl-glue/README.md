# Module: `etl-glue`

The Glue transform job (bronze -> silver), with the script location parameterized (no hard-coded S3 path).

**Status:** placeholder skeleton created in **PR-006**. Resources land here in **PR-011**
(migrated from `terraform-lottery/Prod/` via `terraform state mv`, so no resource is
recreated).

## Files
- `main.tf` — resources (empty until PR-011)
- `variables.tf` — module inputs
- `outputs.tf` — module outputs
