# Module: orchestration
# Intent: Step Functions state machine + the single weekly EventBridge trigger that drives the pipeline.
#
# STATUS: placeholder skeleton (PR-006). No resources yet.
# Resources are migrated here from terraform-lottery/Prod/ in PR-012 via
# `terraform state mv` (no resource churn). See roadmap.md for the exact plan.

# TODO PR-012: move this module's resources in and wire the root caller.
