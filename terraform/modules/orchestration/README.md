# Module: `orchestration`

Step Functions state machine + the single weekly EventBridge trigger that drives the pipeline.

**Status:** placeholder skeleton created in **PR-006**. Resources land here in **PR-012**
(migrated from `terraform-lottery/Prod/` via `terraform state mv`, so no resource is
recreated).

## Files
- `main.tf` — resources (empty until PR-012)
- `variables.tf` — module inputs
- `outputs.tf` — module outputs
