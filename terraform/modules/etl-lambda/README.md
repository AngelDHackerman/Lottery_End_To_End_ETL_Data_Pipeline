# Module: `etl-lambda`

The extractor Lambda function (scrapes loteria.org.gt) and its configuration.

**Status:** placeholder skeleton created in **PR-006**. Resources land here in **PR-010**
(migrated from `terraform-lottery/Prod/` via `terraform state mv`, so no resource is
recreated).

## Files
- `main.tf` — resources (empty until PR-010)
- `variables.tf` — module inputs
- `outputs.tf` — module outputs
