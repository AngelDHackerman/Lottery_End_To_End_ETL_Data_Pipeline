# Module: etl-lambda
# Intent: The extractor Lambda function (scrapes loteria.org.gt) and its configuration.
#
# STATUS: placeholder skeleton (PR-006). No resources yet.
# Resources are migrated here from terraform-lottery/Prod/ in PR-010 via
# `terraform state mv` (no resource churn). See roadmap.md for the exact plan.

# TODO PR-010: move this module's resources in and wire the root caller.
