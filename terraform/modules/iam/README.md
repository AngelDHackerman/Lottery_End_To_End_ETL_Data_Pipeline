# Module: `iam`

IAM roles and policies for Lambda, Glue, Step Functions, EventBridge and SageMaker; personal IAM-user attachments gated behind a variable.

**Status:** placeholder skeleton created in **PR-006**. Resources land here in **PR-009**
(migrated from `terraform-lottery/Prod/` via `terraform state mv`, so no resource is
recreated).

## Files
- `main.tf` — resources (empty until PR-009)
- `variables.tf` — module inputs
- `outputs.tf` — module outputs
