# Module: iam
# Intent: IAM roles and policies for Lambda, Glue, Step Functions, EventBridge and SageMaker; personal IAM-user attachments gated behind a variable.
#
# STATUS: placeholder skeleton (PR-006). No resources yet.
# Resources are migrated here from terraform-lottery/Prod/ in PR-009 via
# `terraform state mv` (no resource churn). See roadmap.md for the exact plan.

# TODO PR-009: move this module's resources in and wire the root caller.
